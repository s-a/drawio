{
  "name": "drawio",
  "version": "1.0.7",
  "description": "Plot serial chart data to an image and save it to filesystem from shell.",
  "main": "lib/index.js",
  "bin": {
    "drawio": "lib/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bump": "npm version patch && git push && git push --tags && npm publish"
  },
  "engines": {
    "node": ">=8.7.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/s-a/drawio.git"
  },
  "author": "s-a",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/s-a/drawio/issues"
  },
  "homepage": "https://github.com/s-a/drawio#readme",
  "dependencies": {
    "n-cli": "^1.2.16",
    "puppeteer": "^0.12.0"
  }
}
